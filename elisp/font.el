;;; font --- Summary
;;; Commentary:
;;; Code:
(add-to-list 'default-frame-alist '(font."Inconsolata 9"))
(set-face-attribute 'default t :font "Inconsolata 9")

(add-hook 'whitespace-mode t)
;;;(add-to-list 'whitespace-style 'space-mark)
;;;(add-to-list 'whitespace-style 'tab-mark)
;;;(add-to-list 'whitespace-style 'newline-mark)
;;;(global-font-lock-mode 1)

;;;(require `visws)

;;; font.el ends here
